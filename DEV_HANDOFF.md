# 《小小世界》V1.0 本地开发交接包

> 快照日期：2026-09-17
>
> 这是后续本地开发的基线版本。设计方案、当前源码、已实现能力、未实现能力、架构约束和运行方式都集中在本目录。

## 目录

- `docs/01-设计方案-V1.0.md`：前期 1～51 项设计决策的完整整理。
- `docs/02-当前进度与未完成项.md`：按“设计目标 / 当前源码”对照，明确哪些已经实现、哪些只是原型、哪些还没开始。
- `docs/03-本地开发交接.md`：Windows 本地运行、开发命令、推荐后续开发顺序、排错方式。
- `docs/04-架构规范.md`：核心数据流、边界、禁止依赖和不变量。
- `docs/06-绘本视觉改版与验证.md`：2026-09-17 本机部署后的视觉改版、交互变化和实测结果。
- `src/`：当前源码。
- `tests/`：当前自动化测试。
- `install.bat / 一键安装.bat`：Windows 安装。
- `start.bat / 启动游戏.bat`：Windows 启动。

## 当前状态结论

这个包已经不是单纯的设计稿，而是一个可以继续本地开发的 TypeScript/Vite/PixiJS 原型。

当前源码已经包含：

- 家 / 公园 / 森林三个场景概念与基本绘制
- 角色移动
- 物体点击、靠近后交互的基础逻辑
- 拾取 / 放下 / 携带
- 种子种植
- 浇水
- 植物离线成长与阶段变化
- 小动物基础自主移动
- 问答面板与知识节点
- 听声找动物小游戏
- 冰融化实验
- 探索机会提示
- 本地世界保存 / 导出 / 删除
- 家长空间
- 基础音效与事件反馈
- Windows 一键安装/启动脚本

2026-09-17 后续更新：已安装依赖并完成本机运行验证；三个场景、角色和道具已替换为绘本风插画，新增响应式镜头、发现册、种植反馈和补水动作。详见 `docs/06-绘本视觉改版与验证.md`。

与冻结的 V1.0 架构相比，当前源码仍属于“体验原型”。正式的 `ActionValidator`、统一 `InteractionSystem`、`SceneManager`、`Collision`、完整 `ActionHandler` 分层、结构化 AI Action、完整探索状态机等尚未按最终架构全部落地。

## 最重要的后续原则

1. 不推翻现有产品设计。
2. 不把 AI 直接接入 WorldState。
3. 不把 PixiJS 类型或显示对象塞进领域状态。
4. 逐步把 `App.ts` 中目前偏集中的逻辑拆回前面已经确定的模块边界。
5. 优先补“可玩闭环”和“可测试性”，其次再扩内容数量。
